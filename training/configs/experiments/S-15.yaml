# S-15 Model: obs_choice=sensor, num_sensors=15

env:
  obs_choice: "sensor"

sensor:
  num_sensors: 15

paths:
  results_dir: "experiments/S-15/results"
  model_dir: "experiments/S-15/models"
  log_dir: "experiments/S-15/tb_logs"
