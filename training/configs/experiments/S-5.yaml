# S-5 Model: obs_choice=sensor, num_sensors=5

env:
  obs_choice: "sensor"

sensor:
  num_sensors: 5

paths:
  results_dir: "experiments/S-5/results"
  model_dir: "experiments/S-5/models"
  log_dir: "experiments/S-5/tb_logs"
