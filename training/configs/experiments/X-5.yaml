# X-5 Model: obs_choice=sector, num_sensors=5

env:
  obs_choice: "sector"

sensor:
  num_sensors: 5

paths:
  results_dir: "experiments/X-5/results"
  model_dir: "experiments/X-5/models"
  log_dir: "experiments/X-5/tb_logs"
